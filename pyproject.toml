[project]
name = "kvstore"
version = "0.1.0"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "spec(slug): maps tests to docs/spec done-when slugs",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pyyaml>=6.0",
]
