# Hello World

A repository-root Python script that prints a greeting when executed.

#### ✅ Hello World

Create and maintain a minimal `hello.py` script.

**Done when:**
- [x] `hello.py` exists at the repository root <!-- slug: hello-world.root.exists -->
- [x] Running `python hello.py` prints exactly `Hello, World!` <!-- slug: hello-world.root.output -->
